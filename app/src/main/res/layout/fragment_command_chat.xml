<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_neuro_gradient"
    android:padding="8dp">

    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageButton
            android:id="@+id/Menu"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background='@android:color/transparent'
            android:src="@drawable/ic_menu"
            android:contentDescription="Открыть меню"
            app:tint="#6A5ACD" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Command Chat"
            android:textSize="20sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:layout_marginStart="12dp"
            android:fontFamily="sans-serif-condensed"/>
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/chatsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintBottom_toTopOf="@id/createChatButton"
        tools:listitem="@layout/item_chat"/>

    <LinearLayout
        android:id="@+id/emptyState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/chatsRecyclerView"
        app:layout_constraintBottom_toBottomOf="@id/chatsRecyclerView"
        app:layout_constraintStart_toStartOf="@id/chatsRecyclerView"
        app:layout_constraintEnd_toEndOf="@id/chatsRecyclerView">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="У вас пока нет чатов."
            android:textColor="#CCCCCC"
            android:textSize="18sp"/>

        <Button
            android:id="@+id/createFirstChatButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Создать первый чат"/>
    </LinearLayout>


    <Button
        android:id="@+id/createChatButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Создать новый чат"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/drawerContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <View
            android:id="@+id/drawerOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#80000000"
            android:alpha="0"
            android:visibility="visible"
            android:clickable="true"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <LinearLayout
            android:id="@+id/drawerPanel"
            android:layout_width="@dimen/drawer_panel_width"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="#1E1B24"
            android:paddingTop="24dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:translationX="@dimen/drawer_panel_width">

            <Button
                android:id="@+id/aiChatButton"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="AI Chat"
                android:textColor="#FFFFFF"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:textSize="18sp"
                android:drawablePadding="16dp"
                android:gravity="start|center_vertical"
                android:paddingStart="24dp"
                android:drawableStart="@drawable/ic_ai"
                style="@style/TextAppearance.AppCompat.Widget.Button"/>

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#454059"
                android:layout_marginVertical="8dp"/>

            <Button
                android:id="@+id/commandChatButton"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="Command Chat"
                android:textColor="#FFFFFF"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:textSize="18sp"
                android:drawablePadding="16dp"
                android:gravity="start|center_vertical"
                android:paddingStart="24dp"
                android:drawableStart="@drawable/ic_command"
                style="@style/TextAppearance.AppCompat.Widget.Button"/>

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#454059"
                android:layout_marginVertical="8dp"/>

            <Button
                android:id="@+id/ProfileButton"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="Profile"
                android:textColor="#FFFFFF"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:textSize="18sp"
                android:drawablePadding="16dp"
                android:gravity="start|center_vertical"
                android:paddingStart="24dp"
                android:drawableStart="@drawable/ic_command"
                style="@style/TextAppearance.AppCompat.Widget.Button"/>
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
